ROUTEROS_URL=http://localhost:8080
ROUTEROS_USER=admin
ROUTEROS_PASSWORD=
ROUTEROS_VERIFY_SSL=false

# Multi-router: set to path of routers.json (overrides single-router env vars above)
# ROUTEROS_CONFIG=routers.json
